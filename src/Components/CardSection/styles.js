import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  containerStyle: {
    justifyContent: 'flex-start',
    flexDirection: 'row',
    borderColor: '#FFF',
    borderWidth: 0.4,
    borderRadius: 50,
    margin: 2
  }
});
